spring:
  application:
    name: reclamation-service-test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

# Configuration minimale pour les tests
logging:
  level:
    root: INFO
    com.restaurant.reclamations: DEBUG
